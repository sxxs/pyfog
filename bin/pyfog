#!/usr/bin/env python3
"""Run pyfog from a checkout without installing it."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))
from pyfog.cli import main  # noqa: E402

sys.exit(main())
