# Drop-in written by pyfog's systemd/install.sh for every unit in
# fog.target. PartOf makes stop and restart of fog.target reach this
# unit; the slice groups the processes for systemctl status fog.slice.
[Unit]
PartOf=fog.target

[Service]
Slice=fog.slice
